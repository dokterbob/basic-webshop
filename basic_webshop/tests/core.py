from webshop.core.tests import *

class SimpleTest(CoreTestBase):
    pass
    # def test_basic_product(self):
    #     """ Test if we can create and save a simple product. """
    #     p = self.product_class(name='Banana', slug='banana', price="15.00")
    #     p.description = 'A nice piece of fruit for the whole family to enjoy.'
    #     p.save()

